services:
  postgres:
    image: postgres:16
    container_name: movieflix-db
    environment:
      POSTGRES_DB: movieflix
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5433:5432"
    volumes:
      - movieflix-data:/var/lib/postgresql/data

volumes:
  movieflix-data: